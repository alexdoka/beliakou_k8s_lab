apiVersion: v1
kind: Pod
metadata:
  name: redis
  labels:
    name: redis
spec:
  containers:
  - name: redis
    image: redis:5.0.6-alpine3.10
    resources:
      limits:
        memory: "64Mi"
        cpu: "100m"

